<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= event.id ? 'Edit' : 'Add' %> Event</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <h1><%= event.id ? 'Edit' : 'Add' %> Event</h1>
    <form action="<%= event.id ? '/events/' + event.id + '/edit' : '/events/new' %>" method="POST">
      <label>Title:
        <input type="text" name="title" value="<%= event.title || '' %>" required>
      </label><br><br>
      <label>Description:<br>
        <textarea name="description" required><%= event.description || '' %></textarea>
      </label><br><br>
      <label>Date:
        <input type="date" name="date" value="<%= event.date || '' %>" required>
      </label><br><br>
      <label>Time:
        <input type="time" name="time" value="<%= event.time || '' %>" required>
      </label><br><br>
      <label>Location:
        <input type="text" name="location" value="<%= event.location || '' %>" required>
      </label><br><br>
      <button type="submit">Save</button>
    </form>
    <p><a href="/events">← Back to list</a></p>
  </body>
</html>
